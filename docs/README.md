# Documentation

This template is a starting point for a NestJS HTTP API, not a full product stack.

**Included:** HTTP server defaults that are easy to run locally and in containers (Fastify, config, logs, validation, errors, optional OpenAPI, Docker).

**Not included:** authentication and persistence. Different projects need different auth and databases, so those choices stay out of this starter.

| Doc                                   | Contents                               |
| ------------------------------------- | -------------------------------------- |
| [Getting started](getting-started.md) | Install, env file, run, scripts, tests |
| [Configuration](configuration.md)     | Env vars, Joi schema, `example.env`    |
| [Features](features.md)               | What is wired in `src/` and why        |
| [Request flow](request-flow.md)       | Path of an HTTP request                |
| [Docker](docker.md)                   | Image and Compose                      |

When you add a feature or change a flow, update these files so they stay a reliable knowledge base.
