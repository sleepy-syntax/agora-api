{
    "version": "0.0.0",
    "configurations": [
        {
            "type": "node",
            "request": "attach",
            "name": "Attach NestJS WS",
            "port": 9229,
            "restart": true,
            "cwd": "${workspaceFolder}/src",
            "remoteRoot": "/usr/src/app"
        }
    ]
}
